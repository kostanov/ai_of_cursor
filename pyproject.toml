[project]
name = "ai-of-cursor"
version = "0.1.0"
description = "Утилиты пользователей (SQLite) и Flask HTTP API"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "flask>=3.1.3",
]

[dependency-groups]
dev = [
    "ruff>=0.15.15",
]
